import { PagingWithoutContent } from '@/types/common';

export const DEFAULT_PAGING: PagingWithoutContent = {
  numberOfItems: 0,
  pageNumber: 1,
  pageSize: 10,
  totalItems: 0,
  totalPages: 1,
};
